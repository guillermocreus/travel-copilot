{
  "name": "travel-copilot-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "setup-db": "node scripts/setup-db.js"
  },
  "dependencies": {
    "openai": "^4.28.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.2.2",
    "wrangler": "^3.28.2"
  }
} 